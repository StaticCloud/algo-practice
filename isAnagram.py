def isAnagram(strA, strB):
    # create a dictionary with unique keys
    # iterate over the first string and add each letter to the dictionary
    # do the same with the second string
    # them check if each value in the dictionary is 2
    